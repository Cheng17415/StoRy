app:
  jwt:
    secret: 01234567890123456789012345678901testsec
    expiration-ms: 3600000
  google:
    client-id: ""
  upload:
    dir: ${java.io.tmpdir}/story-test-uploads
  supabase:
    url: https://example.supabase.co
    service-role-key: test-service-role-key
    bucket: imagenes

spring:
  datasource:
    url: jdbc:h2:mem:storytest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
